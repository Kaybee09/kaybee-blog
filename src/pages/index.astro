---
import "../styles/global.css";
import { getCollection } from "astro:content";

const posts = (await getCollection("blog"))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 10);

const projects = [
  {
    title: "AWS Landing Page Deployment",
    description:
      "Provisioning a Linux server on AWS, configuring networking, SSH, and deploying a landing page.",
    url: "/projects/aws-landing-page-deployment/",
  },
  {
    title: "Linux Server Automation",
    description:
      "Bash scripts and practical Linux notes for automating server tasks.",
    url: "/projects/linux-server-automation/",
  },
  {
    title: "Docker + GitHub Actions App",
    description:
      "A full-stack application prepared with Docker, CI/CD, and cloud deployment.",
    url: "/projects/docker-github-actions-app/",
  },
];

const formatDate = (date) =>
  new Date(date).toLocaleDateString("en-GB", {
    day: "2-digit",
    month: "short",
    year: "numeric",
  });
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="description"
      content="Kaybee writes about cloud engineering, Linux, AWS, DevOps, Bash scripting, and real-world projects."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Kaybee Dev — Cloud Engineering Blog</title>

    <script defer src="/theme.js"></script>
  </head>

  <body>
    <main class="page">
      <button class="theme-toggle" id="themeToggle" aria-label="Toggle dark mode">
        🌙
      </button>

      <header class="hero">
        <h1>Kaybee Dev</h1>

        <p>
          Cloud engineering, Linux, AWS, DevOps, and real project notes.
        </p>

        <div class="social-links" aria-label="Social links">
          <a href="mailto:kaybeelateef@gmail.com" title="Email">email</a>
          <a href="https://github.com/YOUR_GITHUB_USERNAME" target="_blank" rel="noreferrer" title="GitHub">github</a>
          <a href="https://www.linkedin.com/in/kaybee/" target="_blank" rel="noreferrer" title="LinkedIn">linkedin</a>
          <a href="https://www.tiktok.com/@YOUR_TIKTOK_USERNAME" target="_blank" rel="noreferrer" title="TikTok">tiktok</a>
          <a href="https://www.instagram.com/YOUR_INSTAGRAM_USERNAME" target="_blank" rel="noreferrer" title="Instagram">instagram</a>
          <a href="/rss.xml" title="RSS">rss</a>
        </div>
      </header>

      <hr />

      <nav class="main-nav" aria-label="Main navigation">
        <a href="/">blog</a>
        <a href="#projects">projects</a>
        <a href="/about/">about</a>
        <a href="/resume/">resume</a>
      </nav>

      <section class="intro">
        <p>
          I use this site to document what I am learning and building as I grow
          in cloud engineering. You will find notes on Linux, AWS, Bash,
          Docker, CI/CD, and real projects.
        </p>
      </section>

      <section class="posts">
        {posts.length > 0 ? (
          posts.map((post) => (
            <article class="post-card">
              <header class="post-header">
                <h2>
                  <a href={`/blog/${post.slug}/`}>{post.data.title}</a>
                </h2>

                <div class="post-meta">
                  <span>{formatDate(post.data.pubDate)}</span>

                  {post.data.tags && (
                    <div class="tags">
                      {post.data.tags.map((tag) => (
                        <span>{tag}</span>
                      ))}
                    </div>
                  )}
                </div>
              </header>

              <p>{post.data.description}</p>

              <a class="read-more" href={`/blog/${post.slug}/`}>
                ... read more
              </a>
            </article>
          ))
        ) : (
          <article class="post-card">
            <header class="post-header">
              <h2>Building My First AWS Linux Server</h2>

              <div class="post-meta">
                <span>07 Jun 2026</span>

                <div class="tags">
                  <span>aws</span>
                  <span>linux</span>
                  <span>cloud</span>
                </div>
              </div>
            </header>

            <p>
              My notes from provisioning a Linux server on AWS, connecting with
              SSH, configuring security groups, and deploying a landing page.
            </p>

            <a class="read-more" href="#">
              ... read more
            </a>
          </article>
        )}
      </section>

      <section class="projects-section" id="projects">
        <h2>Projects</h2>

        <div class="project-list">
          {projects.map((project) => (
            <article class="project-item">
              <h3>
                <a href={project.url}>{project.title}</a>
              </h3>

              <p>{project.description}</p>
            </article>
          ))}
        </div>
      </section>

      <footer class="footer">
        <p>
          © 2026 Kaybee ·
          <a href="/rss.xml">rss</a>
        </p>
      </footer>
    </main>
  </body>
</html>